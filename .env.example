# PostgreSQL connection string (Vercel Postgres / Neon / Supabase, etc.)
# Use the DIRECT (non-pooled) URL so migrations run at build time.
DATABASE_URL="postgres://user:password@host:5432/dbname?sslmode=require"
