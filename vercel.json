{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && (prisma migrate deploy || echo 'Aviso: DATABASE_URL ausente no build; a migracao sera aplicada no proximo deploy com o banco conectado') && next build",
  "framework": "nextjs"
}
